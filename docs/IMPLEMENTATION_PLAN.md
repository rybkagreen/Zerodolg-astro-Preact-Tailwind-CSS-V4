# Implementation Plan — ZeroDolg

> Версия: 1.1 · 14.07.2026 · Статус: Active ⚠️ Проект **в production**. Это не
> greenfield-фазы (как в Shaliko), а **рабочие потоки** над живым сайтом.
> Решения/версии — `ARCHITECTURE.md`. Сметы — файл `Смета_ZeroDolg.xlsx` (у
> владельца, не в этом репозитории). ⚠️ Начиная с v1.1: DoD-строки, ссылавшиеся
> на `npm run test`, скорректированы — этого скрипта нет в репозитории (см.
> `ARCHITECTURE.md §1`, `CLAUDE.md`). Добавлен раздел Backlog по итогам
> housekeeping-промта (PROMPT_02, 14.07.2026).

## Обзор

Сайт zerodolg.ru работает с октября 2025. Работа делится на 3 потока: техдолг из
аудита (WS-1) · премиум-редизайн главной (WS-2) · поток корректировок (WS-3).
Каждый крупный кусок = один декларативный промт под агента (цель + DoD +
acceptance-контракт), STOP на границе, fresh session per крупный блок.

## WS-0 — Репо-хаускипинг и тулинг `[x]`

- **Цель:** порядок в репозитории (документация, `.gitignore`, env-доки, Claude
  Code конфиг), без изменения прод-поведения сайта.
- **Сделано (PROMPT_02, 14.07.2026):** методички разложены по
  корню/`docs/`/`docs/_planner` (gitignored); старый `docs/` заархивирован в
  `docs.archive/` (gitignored); `.gitignore` актуализирован (`.env*`, `tmp/`,
  `docs.archive/`, `docs/_planner/`, локальная часть агентской памяти);
  `.env.example`/`ARCHITECTURE.md §3` синхронизированы с фактическими ключами
  `.env`; `.claude/settings.json` добавлен (shared, tracked); аудит
  зависимостей/SAST/секретов проведён.
- **DoD:** `git check-ignore` подтверждает игнор ключевых путей; real CI
  (`type-check`+`lint`+ `build`) зелёный; TruffleHog (реальный, не репо-скрипт)
  чист; PR открыт.
- **STOP:** после мержа PR housekeeping-ветки.

## WS-1 — Техдолг из аудита `[ ]`

- **Цель:** закрыть конкретные дефекты, найденные при обходе сайта (лист
  «Найденные правки»).
- **Scope (по приоритету):**
  1. **[Высокий]** Починить сломанную статью `/blog/blog-bankrotstvo-pensionera`
     (не парсится frontmatter, заголовок «Без названия»).
  2. **[Высокий]** Актуализировать `sitemap.xml` (+7 отсутствующих URL) и
     убрать/починить осиротевший URL.
  3. **[Высокий]** Ручная проверка мобильной адаптивности + фиксы (аудит
     визуально не подтвердил).
  4. **[Средний]** Интегрировать `/privacy/`, `/terms/` в общий шаблон
     (шапка/футер/брендинг).
  5. **[Низкий]** Убрать дублирование виджета обратного звонка.
  6. **[Опц.]** Унифицировать ~12 форм/модалок в один конфигурируемый компонент
     (окупается на будущих правках).
- **DoD:** статья рендерится корректно; sitemap валиден и полон; мобильный вид
  проверен и без поломок; `npm run type-check && npm run lint && npm run build`
  зелёные (⚠️ без `&& npm run test` — скрипта нет, см. `ARCHITECTURE.md §1`);
  PROTECTED-дифф контролируем.
- **Смета:** 66–159 тыс ₽ (без п.6 — 42–99 тыс).
- **STOP:** после каждого высокоприоритетного фикса (осмысленная граница) или
  связкой п.1+п.2.

## WS-2 — Премиум-редизайн главной `[ ]`

- **Цель:** переработать главную по дизайн-брифу — от давления к спокойной
  экспертизе.
- **Источник:** `Дизайн-бриф_ZeroDolg_главная.html` (палитра, типо-шкала,
  отступы, карта секций, a11y; артефакт у владельца, не трекается в этом
  репозитории).
- **Этапы (порядок по влиянию на доверие):**
  - **P1 · Быстрые победы** — убрать фейковые триггеры (счётчик «онлайн»,
    сломанный таймер, зацикленный тикер), keyword-stuffing; фикс загрузки фото
    эксперта; Федресурс как доказательство.
  - **P2 · Hero и тон** — переписать копирайт (сдержанно, без «гарантированно»),
    новая композиция, одна форма с минимумом полей.
  - **P3 · Дизайн-система** — токены палитры (тил-акцент), типо-шкала, отступы;
    применить по блокам.
  - **P4 · Структура** — сократить до 6–7 секций, объединить дубли, один целевой
    CTA, отзывы сеткой.
  - **P5 · Доступность и семантика** — лендмарки, alt/aria, focus-state,
    WCAG-контраст, удаление SEO-мусора.
- **DoD:** каждый этап — визуально проверен (desktop + mobile), a11y не
  регрессировала, CSP цела, real CI (`type-check`+`lint`+`build`) зелёный.
- **Смета:** приоритетный старт (P1+P2) — 123–258 тыс ₽; полный — 447–930 тыс ₽;
  без дизайн-макетов (если дизайн свой) — 357–750 тыс ₽.
- **STOP:** на границе каждого этапа (P1…P5) + ревью владельца перед выкаткой на
  прод.
- **Blocker:** согласование направления/макета заказчиком.

## WS-3 — Поток корректировок `[ ]`

- **Цель:** обрабатывать разовые правки по запросам заказчика (ТЗ пока нет).
- **Механика:** каждая правка оценивается по прайсу (лист «Корректировка»); мало
  и разово — почасовая, мин. чек 1 час; поток — абонемент (лист «Абонемент»).
- **DoD:** правка внедрена, покрыта проверкой, real CI зелёный,
  `deploy:checklist` пройден.
- **STOP:** по завершении правки/пакета.

> Легенда: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

## Backlog (BL)

| ID     | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Приоритет                                        | Поток          | Статус |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ | -------------- | ------ |
| BL-001 | `tools/trufflehog-scan.js` даёт ложноположительный/недостоверный результат («чисто»), когда сам TruffleHog CLI недоступен через `npx` — скрипт не отличает «нет находок» от «инструмент не запустился». Нужен отдельный узкий фикс-промт: либо жёстко фейлить при недоступном тулинге, либо переключиться на детерминированный вызов (Docker-образ). Реальный TruffleHog (Docker `trufflesecurity/trufflehog:latest`, все ветки, 126 коммитов) прогнан 14.07.2026 — 0 verified/unverified находок, репо чист.                                                                                                                                                                                                                                                                                                                        | Высокий (публичный репо — ложное «чисто» опасно) | Tooling        | Open   |
| BL-002 | **Подтверждено 14.07.2026:** `tools/semgrep-scan.js` — тот же паттерн, что BL-001. `npx semgrep` резолвится в чужой npm-пакет-сквоттер `semgrep@0.0.1` без исполняемого файла — реальный semgrep вообще не запускается через `npx`/`npm run tools:semgrep`, скрипт либо падает, либо (нужно перепроверить) может отрапортовать «чисто» ложно. Реальный semgrep (через отдельный Python venv + PyPI `semgrep` 1.169.0) прогнан 14.07.2026 — см. BL-008/BL-009 ниже за находки. Тот же фикс, что BL-001 (детерминированный вызов, не `npx`).                                                                                                                                                                                                                                                                                           | Высокий (подтверждено)                           | Tooling        | Open   |
| BL-003 | `.github/workflows/ci.yml`: триггеры на `main`/`develop`, а фактический дефолт — `master` (workflow, вероятно, никогда не запускается на реальных PR); плюс шаг `npm run test` сломан (скрипта нет); плюс GitHub Actions в этом workflow запиненны на мутабельные теги (`@v4` и т.п.), не на commit SHA — semgrep отметил это как supply-chain риск (`yaml.github-actions.security.github-actions-mutable-action-tag`, `.github/workflows/ci.yml:19,22,44,56,59`). Нужен отдельный промт на исправление CI, вне скоупа housekeeping.                                                                                                                                                                                                                                                                                                 | Средний                                          | Tooling/CI     | Open   |
| BL-004 | `PUBLIC_RECAPTCHA_SITE_KEY`/`RECAPTCHA_SECRET` присутствуют в проде и `.env.example`, но использование в `src/` этой сессией не найдено — подтвердить с владельцем, реализовано ли reCAPTCHA на формах фактически, или это задел на будущее.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Средний                                          | Security/Forms | Open   |
| BL-005 | `.env.example` содержит ключи (`CMS_API_BASE`, `CMS_API_KEY`, `PUBLIC_FACEBOOK_PIXEL_ID`, `PUBLIC_GOOGLE_TAG_MANAGER_ID`, `PUBLIC_MAP_API_KEY`), которых нет в боевом `.env` и нет использования в коде; при этом `PUBLIC_GOOGLE_TAG_MANAGER_ID` и реальный `PUBLIC_GTM_ID` выглядят как два имени одной и той же несделанной интеграции (GTM). Не тронуто в этом промте (документация, не рефакторинг) — подтвердить с владельцем, какие из них живые.                                                                                                                                                                                                                                                                                                                                                                              | Низкий                                           | Docs/Env       | Open   |
| BL-006 | `npm audit`: 13 из 42 находок (3 low, 6 moderate, 4 high) остаются после `npm audit fix` — все в devDependency-цепочке `@lhci/cli` (`inquirer`, `external-editor`, `tmp`, `uuid`). `npm audit fix --force` предлагает поставить `@lhci/cli@0.1.0` — это первый релиз пакета (2018), т.е. **откат** с установленной 0.15.1, а не форвард-фикс. Нужно решение владельца: апгрейд апстрима, замена/удаление Lighthouse CI тулинга, либо осознанное принятие риска (dev-only зависимость, не попадает в прод-бандл).                                                                                                                                                                                                                                                                                                                     | Средний (dev-only, не в проде)                   | Tooling/Deps   | Open   |
| BL-007 | `scripts/maintenance/audit-deps.cjs` (за скриптом `npm run maintenance:audit`) сам внутри себя вызывает `npm audit fix`, если находит уязвимости — при находках это не read-only отчёт, а скрипт с побочным эффектом. Стоит сделать fix-шаг опциональным/флагом, чтобы `npm run maintenance:audit` можно было безопасно гонять как чистый аудит без риска неожиданной мутации `package-lock.json`.                                                                                                                                                                                                                                                                                                                                                                                                                                   | Низкий                                           | Tooling        | Open   |
| BL-008 | **Semgrep (14.07.2026, реальный скан, 280 community-правил, 399 файлов): 4 находки уровня ERROR**, требуют узкого security-промта (не в этом housekeeping-проходе): (1) `Dockerfile:59` — финальный стейдж запускает `nginx` без явного non-root `USER`; (2)+(3) `scripts/deploy/deploy-complete.js:42-46` — `spawn(command, args, {shell:true, ...})` в общем `execCommand()`-хелпере, потенциальный command injection (CWE-78), если когда-либо на вход попадёт менее доверенная строка; (4) `scripts/dev/setup-optimization.js:27` — аналогично, `execSync(command)` с переменной строкой через шелл. Во всех трёх JS-находках источник строки на момент проверки — хардкод в этом же файле (не сетевой ввод), но `deploy-complete.js` работает во время реального деплоя с повышенным доверием — заслуживает отдельного прохода. | Высокий (Dockerfile/deploy — прод-путь)          | Security       | Open   |
| BL-009 | Semgrep: 53 находки уровня WARNING (в основном `javascript.lang.security.audit.path-traversal.path-join-resolve-traversal` в `scripts/maintenance/**` и `scripts/deploy/**` — `path.join`/`path.resolve` с переменными сегментами) + 26 уровня INFO (в основном `unsafe-formatstring`/`detect-non-literal-regexp` в логировании, напр. `src/shared/lib/logger.ts`). Не разобраны поштучно в этом промте — пакетный ревью в узком security-промте вместе с BL-008. Полный JSON сохранён в `tmp/` сессии (не в репозитории).                                                                                                                                                                                                                                                                                                           | Низкий/Средний                                   | Security       | Open   |

## Decision log

Locked decisions — в `PROJECT_KNOWLEDGE.md`. Здесь — контекст «почему так».

- **D1** — Стек Astro/Preact/TS/Tailwind/FSD зафиксирован фактом прода (README).
  14.07.2026.
- **D3** — GitHub Actions доступны (в отличие от Shaliko), но триггеры не
  совпадают с фактическим `master` (BL-003) — локальный real CI обязателен
  независимо от статуса Actions. 14.07.2026.
- **D4** — Редизайн главной = «премиум/спокойствие» (тил-акцент, off-white,
  сокращение секций, удаление фейков); источник — дизайн-бриф. 14.07.2026.
- **D5** — Исполнитель = **Claude Code (единственный)**. Подтверждено владельцем
  14.07.2026 (доп. DoD-пункт к PROMPT_02): `.qwen/`, `QWEN.md`, `AGENT.md`,
  `WARP.md` и связанный dead-код (`scripts/package.json` — Qwen
  model-coordination stub) удалены из репозитория, все Qwen/Warp-упоминания и
  «если исполнитель Qwen» оговорки вычищены из `CLAUDE.md`, `ARCHITECTURE.md`,
  `README.md`, `docs/*.md`. Ранее было открытым вопросом (Claude Code default vs
  Qwen) — закрыто.
- **D6** — Housekeeping-промт (PROMPT_02) выполнен исполнителем Claude Code
  14.07.2026; методология адаптирована под реально проверенный стек репозитория,
  ряд утверждений методички (наличие Vitest/ `npm run test`) скорректированы по
  факту репозитория — см. пометки «(проверено)» в
  `ARCHITECTURE.md`/`CLAUDE_CODE_INSTRUCTIONS.md`.

## Conventions

- Промт = поток/этап целиком. Декларативно + DoD + acceptance-контракт.
- STOP на границах этапов. Fresh session per крупный блок.
- 4 принципа + границы FSD. Git policy (feature → PR → `master`; деплой из
  `master`).
- Production changes → gate-checks (`type-check`+`lint`+`build`; `+test` только
  когда раннер реально появится в репозитории); gate упал → STOP.
- PII/security-чувствительное (формы/Bitrix/аналитика) — узкими отдельными
  промтами.
