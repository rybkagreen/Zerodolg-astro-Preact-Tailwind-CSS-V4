// Critical CSS is now handled through Tailwind CSS v4
// All critical styles are included in the main Tailwind build
const criticalCSS = '';

// Ensure we export the actual CSS content
export { criticalCSS };
