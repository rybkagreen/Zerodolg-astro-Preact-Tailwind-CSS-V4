name = "Аудит производительности"
description = "Запустить аудит производительности с помощью Lighthouse"

[command]
type = "shell"
run = "npx lighthouse http://localhost:4321 --output html --output json --output-path ./perf-report"
directory = "."