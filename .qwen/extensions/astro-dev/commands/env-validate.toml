name = "Проверить переменные окружения"
description = "Проверка корректности переменных окружения для проекта"

prompt = "Проверка переменных окружения"

[command]
type = "shell"
run = "npm run env:validate"
directory = "."