name = "Проверить код с помощью линтера"
description = "Запуск ESLint для проверки кода на ошибки и стилистические проблемы"

prompt = "Запуск линтинга кода"

[command]
type = "shell"
run = "npm run lint"
directory = "."